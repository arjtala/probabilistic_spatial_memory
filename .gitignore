*/*.gch
*/*.tmp
*/*.out
*/*.txt
*/build/
build/
.cache/
compile_commands.json
phrases/
targets/
.aider*
.claude/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
datasets/
captures/
annotation/
texput.log

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.log
*.out

# ECCV 2026 author kit (untracked upstream clone — the kit files
# themselves are vendored into journal/paper_drafts/ and tracked so
# the build is portable across machines without a re-clone).
journal/paper-template/
