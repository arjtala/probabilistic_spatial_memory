# LaTeX build artifacts (compile-on-demand; the .tex and the rendered
# figure PDFs are the version-controlled source-of-truth).
# Covers main.* and any variant build (main_4pp.*, main_8pp.*, etc.).
main*.aux
main*.bbl
main*.blg
main*.log
main*.out
main*.pdf
main*.fdb_latexmk
*.fls
*.brf

# Personal de-anonymized build (real author name) — NEVER commit.
main_named.tex
