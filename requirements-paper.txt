# requirements-paper.txt — minimal pin set for the PSM paper reproduction bundle.
#
# Scope: regenerate the headline figures (F2/F3/F6) and Table 2 verdicts from
# the captured JSON artifacts that ship in this repo, via
# `bash scripts/reproduce_paper.sh`. NO GPU, NO API keys, NO encoder
# feature extraction, NO MLLM rerun. Tested against Python 3.12.13.
#
# Install:
#   python -m pip install -r requirements-paper.txt
#
# The full extraction-side dependency tree (torch, transformers,
# projectaria-tools, ffmpeg, etc.) lives in extraction/pyproject.toml
# and is only needed to regenerate the captures themselves.

h3==4.4.1
h5py==3.16.0
numpy==2.4.4
